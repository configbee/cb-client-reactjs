{
  "name": "configbee-react",
  "version": "0.0.2-alpha.2",
  "description": "Configbee ReactJS SDK",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prod-build": "rm -rf dist && npm run build",
    "build-and-publish-alpha": "npm run prod-build && npm publish --tag alpha"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/react": "^18.2.38",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "typescript": "^5.3.2"
  },
  "peerDependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "main": "dist/index.js",
  "module": "dist/index.js",
  "files": [
    "dist"
  ],
  "types": "dist/index.d.ts",
  "dependencies": {
    "configbee-client-core": "^0.0.2-alpha.1"
  }
}
